name: vm-studio-run-binding-nightly

on:
  # Nightly schedule (2 AM UTC) - against staging environment
  schedule:
    - cron: '0 2 * * *'
  
  # Manual trigger for on-demand runs
  workflow_dispatch:
    inputs:
      base_url:
        description: 'Base URL for testing (default: http://localhost:5173)'
        required: false
        default: 'http://localhost:5173'
      browser:
        description: 'Browser to run tests (chromium, firefox, webkit)'
        required: false
        default: 'chromium'

jobs:
  # Job para testar contra ambiente já implantado (staging/prod)
  e2e-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # Só roda se tiver uma URL externa configurada (não localhost)
    if: github.event.inputs.base_url != 'http://localhost:5173' && github.event.inputs.base_url != ''
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "09-tools/web/vm-ui/package-lock.json"
      
      - name: Install dependencies
        working-directory: ./09-tools/web/vm-ui
        run: npm ci
      
      - name: Install Playwright browsers
        working-directory: ./09-tools/web/vm-ui
        run: npx playwright install --with-deps chromium
      
      - name: Run Playwright tests
        working-directory: ./09-tools/web/vm-ui
        env:
          PLAYWRIGHT_BASE_URL: ${{ github.event.inputs.base_url }}
          CI: true
        run: |
          if [ "${{ github.event.inputs.browser }}" != "" ]; then
            npx playwright test --project=${{ github.event.inputs.browser }}
          else
            npx playwright test
          fi
      
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: 09-tools/web/vm-ui/playwright-report/
          retention-days: 7
      
      - name: Upload test traces
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-traces
          path: 09-tools/web/vm-ui/test-results/
          retention-days: 7
      
      - name: Upload screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-screenshots
          path: 09-tools/web/vm-ui/test-results/**/*.png
          retention-days: 7

  # Job de validação estrutural (smoke test) - não requer servidor
  smoke-structure:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "09-tools/web/vm-ui/package-lock.json"
      
      - name: Install dependencies
        working-directory: ./09-tools/web/vm-ui
        run: npm ci
      
      - name: Install Playwright browsers
        working-directory: ./09-tools/web/vm-ui
        run: npx playwright install --with-deps chromium
      
      - name: Validate Playwright config (list tests)
        working-directory: ./09-tools/web/vm-ui
        run: npx playwright test --list
      
      - name: Build frontend
        working-directory: ./09-tools/web/vm-ui
        run: npm run build
